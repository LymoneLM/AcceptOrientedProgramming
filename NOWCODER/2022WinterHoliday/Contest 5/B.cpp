#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    ll n;
    cin>>n;
    if(n%2 == 0)
        cout<<"win-win!";
    else    
        cout<<"Yaya-win!";

    return 0;
}