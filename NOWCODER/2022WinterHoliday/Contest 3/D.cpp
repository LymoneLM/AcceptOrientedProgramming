#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll n;
    cin >> n;
    if (n % 2 == 0) {
        cout << "kou" << endl;
    } else {
        cout << "yukari" << endl;
    }

    return 0;
}