//天梯首选
#include <bits/stdc++.h>
#define mm(a) memset(a,0,sizeof(a))
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    priority_queue<int> q;
    q.pop();//pop无返回值
    map<int,int>::iterator it;
    //开迭代器的语法
    char str[200];
    string s;
    cin.getline(str,100);
    //cin.get();
    getline(cin,s);
    //getline可以与cin.getline混用
    string::iterator its;
    tuple<int,int,int> tp;
    
    return 0;
}