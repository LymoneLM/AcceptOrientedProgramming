#include <bits/stdc++.h>

using ll = long long;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    
    std::ifstream gin("in.cpp",std::ios::in);
    std::ofstream gout("out.json",std::ios::out|std::ios::trunc);

    //todo:写一个把模板转化成code-snippets的软件
    //最好是能直接生成.code-snippets

    return 0;
}